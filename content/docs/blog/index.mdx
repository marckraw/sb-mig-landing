---
title: Blog Ideas
description: Use-case posts planned for the sb-mig documentation site.
---

# Blog ideas

These are the first posts worth writing after the docs skeleton is stable.

## How sb-mig preserves dirty published Storyblok stories

Explain the dual-layer problem, show a dirty published story, then walk through `preserve-layers` dry-run output.

## A safe Storyblok migration workflow

Show a practical sequence:

1. inspect language and published-layer state
2. run a narrow dry run
3. review artifacts
4. run the production migration
5. keep JSONL logs with the release

## Migrating translated Storyblok content without publishing editor drafts

Focus on `--publicationLanguages`, language publish-state maps, and translated `__i18n__` fields.

## Code-first Storyblok component schemas

Explain why component schema files belong in code and how `sync components` fits into review workflows.
